\documentclass{ximera}

\input{../preamble.tex}


\title[Problem 1]{Problem 1}

\begin{document}
\begin{abstract} \end{abstract}
\maketitle


% Extracted from FTC.tex, problem #1
\begin{problem}
  True of False: If $f$ is continuous on the closed interval $[a,b]$,
  then
  $$\dfrac{d}{dx} \left( \int_a^b f(t) \d t \right) = f(x)$$
  \begin{explanation}
    False.  $\dfrac{d}{dx} \left( \int_a^b f(t) \d t \right) = 0$
    since $\int_a^b f(t) \d t$ is a constant and
    $\dfrac{d}{dx} (\text{constant}) = 0$.
  \end{explanation}
\end{problem}



\end{document}
